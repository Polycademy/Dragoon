; Dragoon php.ini file
; Make sure to prevent public access to this file
; See options available here (any except PHP_INI_SYSTEM): http://www.php.net/manual/en/ini.list.php
; The php.ini option `user_ini.filename` must be set to ".user.ini", which is the default!
; See this for syntax http://www.php.net//manual/en/configuration.file.php

[dragoon-code]

short_open_tag = On ; we should allow <? ?> to be used!
default_charset = "UTF-8"

[dragoon-system]

memory_limit = 512M ; our apps are doing alot these days right?
max_execution_time = 160 ; 2.66 minutes, adjust depending on how long your PHP app should be thinking for each response, not appliable to daemons or cli processes

[dragoon-communications]

max_input_time = 220 ; some internet connections are slow
upload_max_filesize = 10M ; you should be AJAX to upload big files anyway
post_max_size = 20M ; needs to be bigger than upload_max_filesize

[dragoon-xdebug]

; these settings allow us to see a full var_dump()!
xdebug.var_display_max_depth = -1
xdebug.var_display_max_children = -1
xdebug.var_display_max_data = -1 